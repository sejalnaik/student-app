import { EmptyToNullDirectveDirective } from './empty-to-null-directve.directive';

describe('EmptyToNullDirectveDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyToNullDirectveDirective();
    expect(directive).toBeTruthy();
  });
});
